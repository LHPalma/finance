info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{LOCALHOST}}/v1/finance/login"
  body:
    type: json
    data: |-
      {
        "email" : "luiz.palma@email.com",
        "password" : "senha123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
