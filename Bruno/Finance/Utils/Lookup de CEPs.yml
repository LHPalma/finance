info:
  name: Lookup de CEPs
  type: http
  seq: 3

http:
  method: GET
  url: "{{localhost}}/v1/finance/utils/cep/lookup/:cep"
  params:
    - name: cep
      value: 01310-200
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
