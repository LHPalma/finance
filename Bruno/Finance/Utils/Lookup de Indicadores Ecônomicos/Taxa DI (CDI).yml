info:
  name: Taxa DI (CDI)
  type: http
  seq: 2

http:
  method: GET
  url: "{{localhost}}/v1/finance/utils/di-rate/current"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
