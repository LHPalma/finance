info:
  name: YahooCharts
  type: http
  seq: 2

http:
  method: GET
  url: "{{localhost}}/v1/finance/utils/stock/yahoo/:ticker"
  params:
    - name: ticker
      value: PETR4
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
